import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fantasy-movies-proto',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
